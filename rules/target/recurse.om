private.language-recurse () =
  c-recurse ()
  ocaml-recurse ()
  export


recurse () =
  curdir		= $(dir .)

  # subdirs with an OMakefile
  section
    .SUBDIRS: $(recurse-subdirs)
      if $(file-exists OMakefile):
        include OMakefile
      language-recurse ()

  export
